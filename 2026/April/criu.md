```bash
sudo apt update

sudo apt install --yes software-properties-common

sudo add-apt-repository --yes ppa:criu/ppa

sudo apt update

sudo apt search criu

sudo apt info criu

sudo apt show criu

sudo apt install --yes criu

criu

crit
```

```bash
sudo apt install --yes software-properties-common

sudo add-apt-repository --yes ppa:criu/ppa

sudo apt install --yes criu
```

```c
#include<stdio.h>
#include<unistd.h>

int main() {
    long long i = 0;

    while(1) {
        printf("%lld\n", ++i);
        fflush(stdout);
        sleep(1);
    }

    return 0;
}
```

